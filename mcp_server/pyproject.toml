[project]
name = "hr-ai-assistant-mcp"
version = "1.0.0"
description = "MCP test server exposing list_users from the HR AI Assistant API"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.9,<2",
    "httpx==0.27.2",
]
